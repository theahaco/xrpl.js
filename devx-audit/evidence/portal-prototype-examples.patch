--- _code-samples/get-started/ts/get-acct-info.ts
+++ _code-samples/devx-after/get-started.ts
@@ -1,3 +1,4 @@
+// PROTOTYPE: requires the built aha DevX SDK fork; not published xrpl 5.3.0.
 import { pathToFileURL } from 'node:url'
 // @chunk {"steps": ["import-node-tag"]}
 // Runtime functions and type-only imports appear separately in the editor.
@@ -39,10 +40,8 @@
   validate(payment)
   const submitted = await client.submitAndWait(payment, { wallet: testWallet })
   // A validated transaction can still fail. Check its result before continuing.
+  // The SDK guarantees parsed metadata after validated inclusion.
   const metadata = submitted.result.meta
-  if (metadata == null || typeof metadata === 'string') {
-    throw new Error('Expected parsed transaction metadata')
-  }
   if (metadata.TransactionResult !== 'tesSUCCESS') {
     throw new Error(`Payment failed: ${metadata.TransactionResult}`)
   }
--- _code-samples/send-xrp/ts/send-xrp.ts
+++ _code-samples/devx-after/send-xrp.ts
@@ -1,3 +1,4 @@
+// PROTOTYPE: requires the built aha DevX SDK fork; not published xrpl 5.3.0.
 import { pathToFileURL } from 'node:url'
 import { Client, xrpToDrops } from 'xrpl'
 import type { Payment, Wallet } from 'xrpl'
@@ -10,8 +11,8 @@
     Destination: receiver.address,
     Amount: xrpToDrops('1')
   } satisfies Payment
-  // Current SDK workaround: name Payment to expose optional autofilled fields.
-  const prepared = await client.autofill<Payment>(payment)
+  // Populated fields are inferred from autofill without a generic argument.
+  const prepared = await client.autofill(payment)
   // autofill returns a new object. Sign that result, not the input object.
   console.log('Prepared fee:', prepared.Fee)
 
@@ -19,15 +20,14 @@
   const signed = sender.sign(prepared)
 
   // Submit the signed blob and wait for a validated ledger result.
+  // The signed blob preserves Payment for the submitAndWait response.
   const confirmed = await client.submitAndWait(signed.tx_blob)
   const metadata = confirmed.result.meta
-  if (metadata == null || typeof metadata === 'string') {
-    throw new Error('Expected parsed payment metadata')
-  }
   if (metadata.TransactionResult !== 'tesSUCCESS') {
     throw new Error(`Payment failed: ${metadata.TransactionResult}`)
   }
   console.log(`Payment confirmed: ${confirmed.result.hash}`)
+  console.log(`Confirmed destination: ${confirmed.result.tx_json.Destination}`)
   console.log(`Receiver balance: ${await client.getXrpBalance(receiver.address)} XRP`)
 }
 
--- _code-samples/issue-mpt-with-metadata/ts/issue-mpt-with-metadata.ts
+++ _code-samples/devx-after/issue-mpt-with-metadata.ts
@@ -1,3 +1,4 @@
+// PROTOTYPE: requires the built aha DevX SDK fork; not published xrpl 5.3.0.
 import { pathToFileURL } from 'node:url'
 import {
   Client,
@@ -41,9 +42,6 @@
   // Submit and check the result
   const created = await client.submitAndWait(issuance, { wallet: issuer })
   const creationMetadata = created.result.meta
-  if (creationMetadata == null || typeof creationMetadata === 'string') {
-    throw new Error('Expected parsed issuance metadata')
-  }
   if (creationMetadata.TransactionResult !== 'tesSUCCESS') {
     throw new Error(`Issuance failed: ${creationMetadata.TransactionResult}`)
   }
@@ -61,8 +59,8 @@
     ledger_index: 'validated'
   })
   const node = entry.result.node
-  // The current SDK returns a ledger-entry union even for an MPT selector.
-  if (node.LedgerEntryType !== 'MPTokenIssuance' || node.MPTokenMetadata == null) {
+  // The MPT selector infers MPTokenIssuance; metadata is still optional.
+  if (node.MPTokenMetadata == null) {
     throw new Error('Expected an MPT issuance with metadata')
   }
   console.log('Metadata:', decodeMPTokenMetadata(node.MPTokenMetadata))
@@ -84,9 +82,6 @@
   } satisfies MPTokenIssuanceSet
   const updated = await client.submitAndWait(update, { wallet: issuer })
   const updateMetadata = updated.result.meta
-  if (updateMetadata == null || typeof updateMetadata === 'string') {
-    throw new Error('Expected parsed update metadata')
-  }
   if (updateMetadata.TransactionResult !== 'tesSUCCESS') {
     throw new Error(`Update failed: ${updateMetadata.TransactionResult}`)
   }
@@ -98,7 +93,7 @@
     ledger_index: 'validated'
   })
   const updatedNode = confirmation.result.node
-  if (updatedNode.LedgerEntryType !== 'MPTokenIssuance' || updatedNode.MPTokenMetadata == null) {
+  if (updatedNode.MPTokenMetadata == null) {
     throw new Error('Expected an updated MPT issuance with metadata')
   }
   console.log('Transfer fee:', updatedNode.TransferFee)
--- _code-samples/create-amm/ts/create-amm-guided.ts
+++ _code-samples/devx-after/create-amm.ts
@@ -1,12 +1,11 @@
+import { pathToFileURL } from 'node:url'
+// PROTOTYPE: requires the built aha DevX SDK fork; not published xrpl 5.3.0.
 import { AccountSetAsfFlags, Client, dropsToXrp, xrpToDrops } from 'xrpl'
-import type { AccountSet, AMMCreate, AMMInfoRequest, Amount, Payment, TrustSet, TxResponse, Wallet } from 'xrpl'
+import type { AccountSet, AMMCreate, AMMInfoRequest, Amount, Payment, TrustSet, ValidatedTxResponse, Wallet } from 'xrpl'
 
 // Every transaction has its own checked result. Validation can include failures.
-function requireSuccess(response: TxResponse): void {
+function requireSuccess(response: ValidatedTxResponse): void {
   const metadata = response.result.meta
-  if (metadata == null || typeof metadata === 'string') {
-    throw new Error('Expected parsed transaction metadata')
-  }
   if (metadata.TransactionResult !== 'tesSUCCESS') {
     throw new Error(`Transaction ${response.result.hash} failed: ${metadata.TransactionResult}`)
   }
@@ -95,7 +94,7 @@
   }
 }
 
-if (require.main === module) {
+if (process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href) {
   main().catch((error: unknown) => {
     console.error(error instanceof Error ? error.message : String(error))
     process.exitCode = 1
